apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'

apply plugin: 'com.github.dcendents.android-maven'
group = 'com.github.anwenh'


android {
    compileSdkVersion 28 //必填 否则报错 compileSdkVersion is not specified.

    defaultConfig {
        minSdkVersion 16 //编辑代码时as不会有报错.
        consumerProguardFiles 'consumer-rules.pro' //混淆设置
    }
    compileOptions {//编辑代码时as不会有报错.
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    //implementation 'androidx.localbroadcastmanager:localbroadcastmanager:1.0.0'

//    api project(':modules:mainpage') //完全依赖
    //implementation project(':modules:module_base') //每个模块都要依赖这个基模块.只是语义上有不同,和放libBase一样

}